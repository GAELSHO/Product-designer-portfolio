---
import BaseLayout from '@/layouts/BaseLayout.astro';
import TituloRotativo from '@/components/TituloRotativo.astro';
import TarjetaProyecto from '@/components/TarjetaProyecto.astro';
import { proyectos } from '@/data/proyectos.js';

const [destacado, ...resto] = proyectos;
---

<BaseLayout title="Gael SHO | Brand Designer">
  <header class="pb-14 sm:pb-20">
    <TituloRotativo />
  </header>

  <section id="proyectos" class="scroll-mt-28">
    <h2 class="sr-only">Proyectos</h2>

    <div class="grid gap-4 sm:gap-6">
      {destacado && <TarjetaProyecto proyecto={destacado} destacada />}

      {
        resto.length > 0 && (
          <div class="grid gap-4 sm:grid-cols-2 sm:gap-6">
            {resto.map((proyecto) => (
              <TarjetaProyecto proyecto={proyecto} />
            ))}
          </div>
        )
      }
    </div>
  </section>

  <section id="sobre-mi" class="mt-24 max-w-2xl scroll-mt-28">
    <h2 class="text-ink-500 text-sm font-medium tracking-widest uppercase">Sobre mí</h2>
    <p class="mt-6 text-lg text-pretty">
      Escribe aquí tu bio: en qué tipo de producto trabajas, cómo trabajas y qué buscas ahora mismo.
    </p>
    <a class="mt-6 inline-block font-medium underline underline-offset-4" href="mailto:gaelsho10@gmail.com">
      gaelsho10@gmail.com
    </a>
  </section>
</BaseLayout>
